#include "src/entities/creators/teams_creator.h"

#include <vector>

#include "src/entities/contestant.h"

namespace tournament {
std::vector<Contestant> TeamsCreator::FromArgs() { return {}; }

std::vector<Contestant> TeamsCreator::FromFile() { return {}; };
}  // namespace tournament